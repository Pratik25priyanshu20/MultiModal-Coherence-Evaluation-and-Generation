# Environment configuration for MultiModal Coherence AI
# Copy this file to .env: cp .env.example .env

# LLM backend (1 = Ollama local, 0 = other)
USE_OLLAMA=1

# Ollama settings (only needed if USE_OLLAMA=1)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen2:7b

# Optional: Gemini API key (if using Gemini backend in future)
# GEMINI_API_KEY=your_key_here

# Freesound API key (for audio dataset expansion)
# Get a free key at: https://freesound.org/apiv2/apply/
# FREESOUND_API_KEY=your_key_here
